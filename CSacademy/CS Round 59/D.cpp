#include <bits/stdc++.h>
using namespace std;

#define MAXN 100005
typedef long long ll;

int N;
ll W, H;
ll L[MAXN];

int main(){
    cin >> N >> W >> H;
    for (int i=1;i<=N;i++)
        cin >> L[i];
    return 0;
    
}